{
  "name": "@orion/sim",
  "version": "0.1.0",
  "description": "ORION simulation core — pure, framework-agnostic, deterministic trading engine",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "license": "UNLICENSED"
}
